/** 
 * detectBrowser.js 
 * 
 * Description: 
 *  Detect browser from a supplied user agent string. 
 */

export default function detectBrowser(userAgent) {
    const browserChain = [
        "brave",
        "vivaldi"
    ]
}